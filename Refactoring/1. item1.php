<?php
  
//To get the area and volume of the rectangle these are the elements: height, width, depth
public function getProperties(array $elements, int $value ){
$targets = ['height', 'weight', 'depth'];
foreach ($elements as $element )
if(in_array($targets, $element){
return $area = $height * $width || $volume = $height * $width * $depth;
}
return $value;
}
  ?>
